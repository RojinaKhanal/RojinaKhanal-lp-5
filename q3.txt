Q3.
Test Scenarios:

For Input
1. Check if the input block accepts the numeric values or not.
2. Check if the input block accepts alphabets or not (negative testing).
3. To test the maximum number of characters the input block can accept (character limit).
4. Test if the input block accepts or rejects white space(space bars).
5. check if the input block is capable of accepting decimals (eg:2.2).

For 'Divide' button
1. The divide button should be enabled once you enter the valid inputs in both the ipnut blocks.
2. Check if the division can be performed by directly pressing on enter button from keyboard without pressing on divide button. (negative testing)
3. Check if the division button is already enabled prior to the input being entered.

For Result
1. Verify the validity of result block; check if result block shows required result with the avaibility of the valid input.
2. To test if the result block shows 'error' message on entering invalid inputs (eg- 5/0, Result-error).
3. Check if result is shown in fraction or in decimal for cases with Input1 smaller than Input2.

Additional
1. Check if the back space button is available to clear entered input.
2. To check if the reset button is available to clear the entire input and result to start a new calculation.